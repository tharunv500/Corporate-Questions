import math

m = int(input("Enter m value: "))
n = int(input("Enter n value: "))

if m > n:
    print(0)
else:
    total = 0
    for i in range(m, n + 1):
        total += math.factorial(i)
    print(total)
